'use strict';

const request = require('request-promise-native');

// This is just an example function
function unlockChapter(title, chapter, txt) {
  // request.post('pastebinUrl', body)
  //   .then(blah blah blah);
}

module.exports = {
  unlockChapter,
};
