'use strict';

/**
 * TODO This should be a base class/function that all other Tasks can extend.
 * Should look something like:
 * 
 * class Task {
 *   constructor() {}
 *   interval() {
 *     return 1 day;  // Indicates that this task should be run daily. 
 *                    // Could also be something like return 12345 so it runs every 12345 seconds
 *   }
 *   action () {
 *      // The actual stuff that needs to be executed. Could be something like the daily check-in
 *   }
 * }
 */
